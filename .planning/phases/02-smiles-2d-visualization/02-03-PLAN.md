---
phase: 02-smiles-2d-visualization
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
  - frontend/src/components/MoleculeCard.tsx
  - frontend/src/components/MoleculeCard.test.tsx
  - frontend/src/styles/App.css
  - frontend/src/styles/MoleculeCard.css
  - frontend/src/index.css
  - docs/IMPLEMENTATION_SUMMARY.md
  - .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md
autonomous: false
requirements:
  - P2-DATA
  - P2-SELECT
  - P2-RENDER
  - P2-STATES
  - P2-CARD
  - P2-ASSET
  - P2-SCOPE
must_haves:
  truths:
    - "Each load selects one random molecule and Pick another always selects a different one (D-01, D-02, D-03)."
    - "Pick another sits below the card and is disabled while depiction is loading or retrying (D-04, D-08)."
    - "The card has an inside-card title above centered 2D artwork and a wrapping labeled SMILES footer (D-09, D-10, D-11, D-12)."
    - "A production Vite preview loads real local RDKit JS/WASM and depicts at least two local SMILES without CDN or backend requests."
  artifacts:
    - path: frontend/src/App.tsx
      provides: "Random initial selection and different-next interaction"
    - path: frontend/src/components/MoleculeCard.tsx
      provides: "MTG-style title, 2D artwork integration, and SMILES footer"
    - path: frontend/src/App.test.tsx
      provides: "P2-SELECT and loading lockout integration tests"
    - path: docs/IMPLEMENTATION_SUMMARY.md
      provides: "Recorded real production-preview smoke evidence"
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/components/MoleculeCard.tsx
      via: "Selected conformer and preview pending callback"
      pattern: "MoleculeCard"
    - from: frontend/src/components/MoleculeCard.tsx
      to: frontend/src/components/Molecule2DPreview.tsx
      via: "Selected SMILES is rendered as card artwork"
      pattern: "Molecule2DPreview"
---

<objective>
Complete the user-facing Phase 2 card and random-selection experience, implementing D-01 through D-04 and D-08 through D-12, then prove real RDKit assets work from a production preview.

Purpose: Deliver the actual frontend-only 2D molecule-card outcome rather than isolated infrastructure.
Output: Random single-card flow, MTG-style card composition, full tests, validation traceability, and browser smoke evidence.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-smiles-2d-visualization/02-CONTEXT.md
@.planning/phases/02-smiles-2d-visualization/02-VALIDATION.md
@.planning/phases/02-smiles-2d-visualization/02-01-SUMMARY.md
@.planning/phases/02-smiles-2d-visualization/02-02-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/MoleculeCard.tsx
@frontend/src/styles/App.css
@frontend/src/styles/MoleculeCard.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace tabs with random different-next selection</name>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/styles/App.css</files>
  <read_first>frontend/src/App.tsx, frontend/src/data/mockMolecules.ts, frontend/src/components/MoleculeCard.tsx, frontend/src/styles/App.css, .planning/phases/02-smiles-2d-visualization/02-CONTEXT.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <behavior>
    - A lazy initializer uses Math.random once to select any of the ten records, including a non-first record (D-01).
    - Ordinary rerenders do not select again.
    - The sole `Pick another` control always chooses an id other than the current id (D-02, D-03).
    - The native button is below the card and disabled while preview work is pending (D-04, D-08).
  </behavior>
  <action>Write App integration tests first, mocking randomness and the preview boundary as needed. Replace saved-structure tabs with a lazy random selected id. Implement different-next selection without retry loops: choose uniformly from the other nine records by mapping a random index around the current index. Render one native `Pick another` button directly below the card, driven by the preview pending callback; keep it visible but disabled during loading/retry. Remove the obsolete navigation and do not label this action as generation.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/App.test.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>Tests prove random initialization, stable rerenders, different-next behavior across boundary indices, below-card placement, and pending-state disablement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose the final MTG-style 2D molecule card</name>
  <files>frontend/src/components/MoleculeCard.tsx, frontend/src/components/MoleculeCard.test.tsx, frontend/src/styles/MoleculeCard.css, frontend/src/index.css, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</files>
  <read_first>frontend/src/components/MoleculeCard.tsx, frontend/src/components/MoleculeViewer3D.tsx, frontend/src/components/Molecule2DPreview.tsx, frontend/src/styles/MoleculeCard.css, frontend/src/index.css, .planning/phases/02-smiles-2d-visualization/02-CONTEXT.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <behavior>
    - Molecule name is inside the card and precedes artwork in DOM order (D-10).
    - Molecule2DPreview fully replaces the old placeholder and no pending-3D copy remains (D-09).
    - Artwork remains centered, padded, responsive, and aspect-ratio preserving (D-11).
    - The labeled SMILES footer remains visible and wraps long text (D-12).
    - The card itself no longer exposes synthetic button semantics.
  </behavior>
  <action>Write card composition tests first. Replace `MoleculeViewer3D` with `Molecule2DPreview`, add the molecule name as the title inside the card immediately before artwork, and pass pending-state changes upward. Remove whole-card click/keyboard selection semantics because D-02 assigns selection to the dedicated control. Adjust component styles and CSS imports so the title/artwork/footer hierarchy is collectible-card-like, the frame is stable, and long SMILES wrap. Update `02-VALIDATION.md` TBD rows with actual task/plan/wave IDs and set `wave_0_complete: true` only after the test harness executes passing tests; leave `nyquist_compliant: false` until the final browser checkpoint passes.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/MoleculeCard.test.tsx src/components/Molecule2DPreview.test.tsx src/App.test.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>Card and integration tests cover D-09 through D-12, the full frontend suite/build pass, and validation rows identify their real tasks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real RDKit assets in the production preview</name>
  <files>docs/IMPLEMENTATION_SUMMARY.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</files>
  <read_first>docs/IMPLEMENTATION_SUMMARY.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <what-built>The complete frontend-only Phase 2 flow backed by pinned local RDKit JavaScript and WASM assets.</what-built>
  <action>Run the automated gate, then pause for the user to complete the browser checks below. After approval, record the supplied evidence and update validation sign-off; do not mark Nyquist compliant before that approval.</action>
  <how-to-verify>Run `cd frontend &amp;&amp; npm test -- --run &amp;&amp; npm run build`, then start `npm run preview -- --host 127.0.0.1`. In a browser with network cache disabled: (1) confirm the application requests `RDKit_minimal.js` and then `RDKit_minimal.wasm`, both return HTTP 200 from the preview origin, and no CDN/backend request occurs; (2) perform the ten-example visual sweep by using `Pick another` and reloading as needed until every named compound has appeared, recording all ten names and checking centered, generously padded, unclipped artwork, readable title, and wrapping SMILES for each; (3) reload at least five times and click `Pick another` at least 20 times, confirming every click changes the molecule and the control never implies generation; (4) with network throttling/cache disabled, verify the button is disabled while the fixed-size loading state is visible; (5) block the WASM request, reload, confirm fallback plus `Retry preview`, unblock and retry successfully; (6) check desktop and a viewport at or below 720px for title placement, centered unclipped SVG, below-card button, and wrapping SMILES. Record command/worktree state, browser, both requested asset URLs/statuses, all ten names from the visual sweep, the five-reload/twenty-click result, responsive/failure observations, and absence of CDN/backend requests in `docs/IMPLEMENTATION_SUMMARY.md`; only then mark P2-ASSET evidence and `nyquist_compliant: true` in `02-VALIDATION.md`.</how-to-verify>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; test -s dist/rdkit/RDKit_minimal.js &amp;&amp; test -s dist/rdkit/RDKit_minimal.wasm</automated>
  </verify>
  <done>The user confirms the same-origin JS-to-WASM request chain, all ten visual checks, five reloads and twenty different-next clicks, loading/recovery behavior, responsive composition, and recorded evidence; validation is then marked compliant.</done>
  <resume-signal>Type "approved" after the evidence is recorded, or describe any failed browser check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local molecule selection → RDKit preview | Selected local SMILES drives asynchronous SVG creation. |
| production static server → browser WASM runtime | Built assets must load from the expected origin and paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | repeated selection | mitigate | Disable selection while pending and prevent stale results from committing. |
| T-02-07 | Tampering | production asset delivery | mitigate | Browser smoke verifies same-origin 200 responses and absence of CDN requests. |
| T-02-08 | Elevation | raw SVG integration | mitigate | Card passes only SMILES to the isolated tested sink; metadata remains React text. |
| T-02-SC | Tampering | npm package assets | mitigate | Exact dependency and copied asset provenance are established in Plans 01-02. |
</threat_model>

## Artifacts this phase produces

- A single-card random-selection experience over exactly ten compounds.
- An MTG-style molecule card with real RDKit 2D SVG artwork and intentional failure states.
- Automated selection/card/state coverage plus recorded real production-preview WASM evidence.

<verification>
Run the full frontend suite/build, complete the production-preview browser procedure, and audit the diff for P2-SCOPE: no backend/API, generation, 3D, persistence, uploads, lookup, auth, queue, or deployment additions.
</verification>

<success_criteria>
- Every locked decision D-01 through D-15 is implemented and tested or browser-verified.
- All P2 requirement IDs pass their automated or production-preview checks.
- Real local RDKit JS/WASM depicts two compounds from a Vite production preview.
</success_criteria>

<output>
Create `.planning/phases/02-smiles-2d-visualization/02-03-SUMMARY.md` when done.
</output>
