---
phase: 02-smiles-2d-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/types/molecule.ts
  - frontend/src/data/mockMolecules.ts
  - frontend/src/data/mockMolecules.test.ts
autonomous: true
requirements:
  - P2-DATA
  - P2-ASSET
  - P2-SCOPE
must_haves:
  truths:
    - "The frontend has exactly 10 unique, familiar, named drug-like compounds available locally (D-13, D-14, D-15)."
    - "The frontend has a deterministic non-watch unit/component test command that does not load real WASM."
    - "RDKit and test dependencies are pinned in the lockfile and require no runtime CDN or backend."
  artifacts:
    - path: frontend/src/data/mockMolecules.ts
      provides: "Typed local catalog of exactly 10 named compounds"
      contains: "MOCK_CONFORMERS"
    - path: frontend/src/data/mockMolecules.test.ts
      provides: "P2-DATA invariant coverage"
    - path: frontend/src/test/setup.ts
      provides: "jsdom matcher and cleanup setup"
    - path: frontend/package-lock.json
      provides: "Pinned RDKit and test dependency graph"
  key_links:
    - from: frontend/src/data/mockMolecules.ts
      to: frontend/src/types/molecule.ts
      via: "Every catalog record satisfies the evolved Conformer contract"
      pattern: "Conformer"
    - from: frontend/vite.config.ts
      to: frontend/src/test/setup.ts
      via: "Vitest jsdom setupFiles configuration"
      pattern: "setup"
---

<objective>
Create the Phase 2 local molecule catalog and Wave 0 validation foundation, covering D-13, D-14, and D-15.

Purpose: Give later depiction and interaction tasks a typed, testable ten-compound source and fast deterministic feedback loop.
Output: Pinned dependencies, Vitest/jsdom setup, evolved molecule type, exactly ten local compounds, and dataset invariant tests.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-smiles-2d-visualization/02-CONTEXT.md
@.planning/phases/02-smiles-2d-visualization/02-RESEARCH.md
@.planning/phases/02-smiles-2d-visualization/02-PATTERNS.md
@.planning/phases/02-smiles-2d-visualization/02-VALIDATION.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/src/types/molecule.ts
@frontend/src/data/mockMolecules.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned RDKit and Wave 0 test tooling</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/src/test/setup.ts</files>
  <read_first>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/tsconfig.json, .planning/phases/02-smiles-2d-visualization/02-RESEARCH.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <action>Before installing, run `npm view @rdkit/rdkit@2025.3.4-1.0.0 version scripts.postinstall` from `frontend`; stop for review if the identity/version differs or an unexpected postinstall script appears. Install `@rdkit/rdkit@2025.3.4-1.0.0` as an exact runtime dependency and version-compatible exact releases of Vitest, jsdom, `@testing-library/react`, `@testing-library/dom`, and `@testing-library/jest-dom` as dev dependencies. Add a non-watch `test` script, configure Vitest with jsdom and the setup file, and initialize DOM matchers plus cleanup in `src/test/setup.ts`. Do not add a CDN, fetch call, server, or backend dependency (P2-ASSET, P2-SCOPE).</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run --passWithNoTests &amp;&amp; npm run build</automated>
  </verify>
  <done>The pinned package graph installs, `npm test -- --run` exits rather than watching, jsdom setup loads, and the existing production build remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and enforce the ten-compound local catalog</name>
  <files>frontend/src/types/molecule.ts, frontend/src/data/mockMolecules.ts, frontend/src/data/mockMolecules.test.ts</files>
  <read_first>frontend/src/types/molecule.ts, frontend/src/data/mockMolecules.ts, frontend/src/components/MoleculeCard.tsx, .planning/phases/02-smiles-2d-visualization/02-CONTEXT.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <behavior>
    - The exported catalog contains exactly 10 records.
    - Every id, name, and SMILES value is non-empty and unique.
    - Every record retains the fields required by the current card/conformer contract.
    - The catalog is entirely local and performs no network lookup.
  </behavior>
  <action>Write the invariant test first, then minimally add required `name` data to the existing type and fixture boundary. Replace the four-record set with ten unique familiar named drug-like compounds per D-13 and D-14. Use this balanced D-15 list unless a SMILES is shown invalid by the real RDKit smoke later: Aspirin, Caffeine, Acetaminophen, Ibuprofen, Naproxen, Lidocaine, Nicotine, Fluoxetine, Diazepam, and Warfarin. Use explicit stable ids and manageable canonical/isomeric SMILES; preserve the card-required conformer fields without treating those fields as Phase 2 generated geometry. Keep `getMockConformerSet` as the local loading boundary and do not perform chemical identity lookup.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/data/mockMolecules.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>The test proves exactly ten unique named local compounds and the evolved type/data compile without changing the frontend-only boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | Third-party runtime and test packages enter the local build. |
| local catalog → future SVG adapter | Fixed SMILES strings become chemistry parser inputs in Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | package installation | mitigate | Verify official scoped package metadata, pin exact versions, and retain the lockfile. |
| T-02-02 | Tampering | local molecule catalog | accept | Records are reviewed, fixed source constants; invalid chemistry is handled inertly in Plan 02. |
| T-02-SC | Tampering | npm installs | mitigate | Apply the research package-legitimacy audit and stop on unexpected install scripts. |
</threat_model>

## Artifacts this phase produces

- A repeatable Vitest/jsdom/React Testing Library harness used by all Phase 2 plans.
- A typed, fixed local catalog of ten unique named drug-like compounds.
- A pinned official RDKit dependency and lockfile, with no runtime network boundary.

<verification>
Run `cd frontend &amp;&amp; npm test -- --run src/data/mockMolecules.test.ts &amp;&amp; npm run build`. Confirm changed paths remain under `frontend/` plus Phase 2 planning artifacts.
</verification>

<success_criteria>
- `npm test -- --run` is deterministic and non-watch.
- Dataset invariants for P2-DATA pass.
- Dependency and build checks for P2-ASSET pass without CDN/backend code.
</success_criteria>

<output>
Create `.planning/phases/02-smiles-2d-visualization/02-01-SUMMARY.md` when done.
</output>
