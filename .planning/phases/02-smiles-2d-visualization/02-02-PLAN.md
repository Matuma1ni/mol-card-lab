---
phase: 02-smiles-2d-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/public/rdkit/RDKit_minimal.js
  - frontend/public/rdkit/RDKit_minimal.wasm
  - frontend/src/lib/rdkit.ts
  - frontend/src/lib/rdkit.test.ts
  - frontend/src/components/Molecule2DPreview.tsx
  - frontend/src/components/Molecule2DPreview.test.tsx
  - frontend/src/styles/Molecule2DPreview.css
autonomous: true
requirements:
  - P2-RENDER
  - P2-STATES
  - P2-ASSET
  - P2-SCOPE
must_haves:
  truths:
    - "A valid selected SMILES renders RDKit-produced SVG from pinned local JS/WASM assets."
    - "Loading, invalid-SMILES, initialization-error, retry, and stale-result states behave exactly as D-05 through D-08 require."
    - "Every temporary RDKit molecule is deleted and failed initialization can be retried."
  artifacts:
    - path: frontend/src/lib/rdkit.ts
      provides: "Resettable singleton loader and disposable SMILES-to-SVG adapter"
      exports: ["loadRDKit", "renderSmilesToSvg"]
    - path: frontend/src/components/Molecule2DPreview.tsx
      provides: "Isolated RDKit lifecycle, SVG sink, and fallback UI"
    - path: frontend/public/rdkit/RDKit_minimal.wasm
      provides: "Locally served production WASM asset"
    - path: frontend/public/rdkit/RDKit_minimal.js
      provides: "Same-origin script that defines initRDKitModule"
    - path: frontend/src/components/Molecule2DPreview.test.tsx
      provides: "P2-STATES and stale-result coverage"
  key_links:
    - from: frontend/src/components/Molecule2DPreview.tsx
      to: frontend/src/lib/rdkit.ts
      via: "Effect invokes narrow adapter and ignores stale completions"
      pattern: "renderSmilesToSvg"
    - from: frontend/src/lib/rdkit.ts
      to: frontend/public/rdkit/RDKit_minimal.js
      via: "BASE_URL-aware singleton script element loads the initializer global"
      pattern: "RDKit_minimal\\.js"
    - from: frontend/src/lib/rdkit.ts
      to: frontend/public/rdkit/RDKit_minimal.wasm
      via: "locateFile resolves Vite BASE_URL local asset"
      pattern: "RDKit_minimal\\.wasm"
---

<objective>
Build the isolated, resource-safe RDKit depiction boundary and all locked loading/failure behavior from D-05 through D-08.

Purpose: Turn local SMILES into trustworthy card artwork without leaking WASM lifecycle details through the app.
Output: Deterministically copied RDKit assets, singleton adapter, preview state component, styles, and focused tests.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-smiles-2d-visualization/02-CONTEXT.md
@.planning/phases/02-smiles-2d-visualization/02-RESEARCH.md
@.planning/phases/02-smiles-2d-visualization/02-PATTERNS.md
@.planning/phases/02-smiles-2d-visualization/02-VALIDATION.md
@.planning/phases/02-smiles-2d-visualization/02-01-SUMMARY.md
@frontend/package.json
@frontend/src/styles/MoleculeViewer3D.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add local RDKit assets and resource-safe adapter</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/public/rdkit/RDKit_minimal.js, frontend/public/rdkit/RDKit_minimal.wasm, frontend/src/lib/rdkit.ts, frontend/src/lib/rdkit.test.ts</files>
  <read_first>frontend/package.json, frontend/vite.config.ts, .planning/phases/02-smiles-2d-visualization/02-RESEARCH.md, .planning/phases/02-smiles-2d-visualization/02-PATTERNS.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <behavior>
    - Concurrent callers append one same-origin RDKit script and share its load promise.
    - Script success resolves the `initRDKitModule` global; script error or a missing global clears cached state for retry.
    - Concurrent callers share one in-flight/successful initialization promise.
    - Initialization rejection clears cached state, allowing a fresh retry.
    - Valid SMILES returns SVG; null/invalid parse maps to a typed invalid result.
    - The molecule object's delete method runs once after success and after SVG generation failure.
  </behavior>
  <action>Write adapter tests with a fake DOM/global and fake RDKit module first. Add a deterministic package script that copies the pinned distribution's `RDKit_minimal.js` and `RDKit_minimal.wasm` into `public/rdkit/` and runs before build; do not load remote assets. Implement a narrow typed adapter that appends one script element whose URL is `${import.meta.env.BASE_URL}rdkit/RDKit_minimal.js`, caches and shares the script-load promise, then invokes the resulting `window.initRDKitModule` global with `locateFile` resolving `${import.meta.env.BASE_URL}rdkit/RDKit_minimal.wasm`. Treat script error and a missing initializer global as initialization failures, remove a failed script element, and clear both relevant failed caches so Retry performs a fresh attempt. Cache one successful module initialization promise, distinguish invalid parse from initialization failure, call `get_svg()`, and always call `delete()` in `finally`. Keep DOM/global/Emscripten details inside this module. Do not add generation, MolBlock handling, 3D code, or backend calls (P2-SCOPE).</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/lib/rdkit.test.ts &amp;&amp; npm run build &amp;&amp; test -s dist/rdkit/RDKit_minimal.js &amp;&amp; test -s dist/rdkit/RDKit_minimal.wasm</automated>
  </verify>
  <done>Adapter lifecycle tests pass and a production build contains non-empty local JS and WASM assets at stable URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement depiction loading, fallback, retry, and race safety</name>
  <files>frontend/src/components/Molecule2DPreview.tsx, frontend/src/components/Molecule2DPreview.test.tsx, frontend/src/styles/Molecule2DPreview.css</files>
  <read_first>frontend/src/lib/rdkit.ts, frontend/src/styles/MoleculeViewer3D.css, frontend/src/components/MoleculeViewer3D.tsx, .planning/phases/02-smiles-2d-visualization/02-CONTEXT.md, .planning/phases/02-smiles-2d-visualization/02-VALIDATION.md</read_first>
  <behavior>
    - Deferred rendering shows a fixed artwork region labeled exactly `Loading molecule…` (D-05).
    - Invalid SMILES shows `2D preview unavailable` plus the SMILES, without Retry or raw error text (D-06).
    - Initialization failure shows the inert fallback and a native `Retry preview` button that starts a fresh loading attempt (D-07).
    - The component reports loading/retrying upward so selection can be disabled (D-08).
    - A result from an older SMILES or unmounted component never replaces current artwork.
  </behavior>
  <action>Write component tests against the mocked adapter first. Implement a small `Molecule2DPreview` state machine for loading, ready, invalid, and initialization error. On prop change or unmount, invalidate stale async completion. Confine raw markup insertion to this component, insert only adapter-produced RDKit SVG, and keep name/SMILES as ordinary React text rather than concatenating metadata into markup. Provide an accessible artwork label. Style a fixed-size centered frame that preserves dimensions in every state and constrains the ready SVG with preserved aspect ratio and generous padding in preparation for D-11.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/lib/rdkit.test.ts src/components/Molecule2DPreview.test.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>All P2-RENDER/P2-STATES unit and component cases pass, raw errors remain hidden, retry is functional, and stale artwork cannot win a race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned RDKit runtime → DOM SVG sink | Generated markup crosses into raw DOM insertion. |
| React selection/effect → shared WASM module | Async work and manually managed WASM heap resources cross lifecycles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering / Elevation | Molecule2DPreview SVG sink | mitigate | Only locally pinned RDKit output reaches the single sink; all metadata remains escaped React text. |
| T-02-04 | Denial of Service | RDKit molecule lifecycle | mitigate | Always delete temporary molecule objects and ignore stale completions. |
| T-02-05 | Tampering | WASM asset resolution | mitigate | Copy exact package assets locally and verify production-build files. |
| T-02-SC | Tampering | npm package assets | mitigate | Plan 01 pins and audits the official package; this plan copies only from that locked dependency. |
</threat_model>

## Artifacts this phase produces

- A resettable singleton same-origin script/module loader and disposable depiction adapter.
- Locally packaged RDKit JavaScript and WASM assets that survive production builds.
- An isolated accessible 2D preview component with loading, invalid, retry, and race-safe states.

<verification>
Run the two focused test files, build, and assert both `dist/rdkit/` assets are non-empty. The final plan performs the required real-browser production-preview smoke.
</verification>

<success_criteria>
- P2-RENDER, P2-STATES, and build-time P2-ASSET checks pass.
- SVG insertion is isolated to one component.
- No API/backend/generation/3D boundary is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-smiles-2d-visualization/02-02-SUMMARY.md` when done.
</output>
