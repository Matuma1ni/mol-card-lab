---
phase: 02-frontend-integration-and-real-data-flow
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - backend/src/generation_service.py
  - backend/src/generate_demo.py
  - backend/src/api.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
user_setup: []
must_haves:
  truths:
    - "CLI and POST /generate call one shared synchronous generation and serialization path."
    - "Only contained relative .mol paths under backend/data/reference_molecules are accepted by the API."
    - "Successful conformers preserve response order and MolBlock while failures appear only in run metadata."
  artifacts:
    - path: backend/src/generation_service.py
      provides: "Shared conformer orchestration for CLI and API"
      exports: [generate_conformer_data]
    - path: backend/src/api.py
      provides: "FastAPI app and synchronous POST /generate"
      exports: [app, GenerateRequest, resolve_reference_mol_path]
  key_links:
    - from: backend/src/api.py
      to: backend/src/generation_service.py
      via: "POST /generate invokes generate_conformer_data"
      pattern: "generate_conformer_data"
    - from: backend/src/generate_demo.py
      to: backend/src/generation_service.py
      via: "CLI main invokes the same generate_conformer_data function"
      pattern: "generate_conformer_data"
    - from: backend/src/generation_service.py
      to: backend/src/serialize.py
      via: "serializes filtered RDKit molecules through mols_to_json"
      pattern: "mols_to_json"
---

<objective>
Implement the Phase 2 backend boundary for decisions D-01 through D-08.

Purpose: Expose real generation over a safe local HTTP endpoint without duplicating chemistry or serialization behavior.
Output: Shared generation service, refactored CLI, and one-route FastAPI application.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-01-SUMMARY.md
@backend/src/generate_demo.py
@backend/src/serialize.py
@backend/src/molecule_utils.py
@backend/src/models.py
@backend/src/config.py
@backend/tests/test_generation_service.py
@backend/tests/test_api.py

<interfaces>
Existing exports:
- `load_reference_molecule(reference_mol_path: str | None) -> tuple[Chem.Mol | None, dict[str, str]]`
- `ConformerGeneratorWrapper.get_instance(edm_weights, adj_mat_weights, diffusion_steps)`
- `ConformerGeneratorWrapper.generate_conformers(reference, n_samples=10, variance=2)`
- `mols_to_json(mols, base_id="conformer", metadata=None) -> str`
- `REFERENCE_MOLECULES_DIR`, `DEFAULT_NUM_CONFORMERS`, `DEFAULT_VARIANCE`, `DEFAULT_DIFFUSION_STEPS`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract one reusable generation and serialization service</name>
  <files>backend/src/generation_service.py, backend/src/generate_demo.py</files>
  <read_first>backend/src/generate_demo.py, backend/src/serialize.py, backend/src/molecule_utils.py, backend/src/models.py, backend/src/config.py, backend/tests/test_generation_service.py, docs/architecture/ADR-0001-reference-molecule-input.md</read_first>
  <action>Create `generate_conformer_data(reference_mol_path=None, reference_path_label=None, n_samples=DEFAULT_NUM_CONFORMERS, variance=DEFAULT_VARIANCE, diffusion_steps=DEFAULT_DIFFUSION_STEPS) -> dict[str, Any]` in `generation_service.py`. Move weight validation, reference loading/validation, generator acquisition, generation, filtering, metadata construction, and existing `mols_to_json` parsing into this function. Preserve generated order while removing every `None` before serialization. Metadata must include `reference_smiles`, `num_requested`, `num_generated`, `num_failed`, `variance`, `diffusion_steps`, `generated_at`, `warnings`, `warning_count`, `reference_source`, `reference_3d_geometry`, and optional relative `reference_path`; `warnings` contains one concise partial-generation message when failures occur and `warning_count` is its length. Keep MolBlock primary and coordinates derived (D-06), and keep provided versus embedded geometry distinct (D-07). Refactor `generate_demo.py` so argument parsing/file writing remain CLI concerns but `main()` calls this service, preserving CLI flags/defaults and output shape (D-01/D-04/D-08). Never expose a resolved absolute API path through metadata.</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/pytest tests/test_generation_service.py -x</automated></verify>
  <acceptance_criteria>
    - `backend/src/generation_service.py` exports `generate_conformer_data` with the exact parameters named in the action.
    - `backend/src/generate_demo.py` calls `generate_conformer_data` and no longer duplicates generator initialization/generation/metadata serialization.
    - `tests/test_generation_service.py` exits 0 without loading model weights.
    - A partial fake result yields only valid conformers in original order, `count == num_generated`, non-empty `molBlock`, and consistent failure/warning metadata.
  </acceptance_criteria>
  <done>CLI generation behavior and response serialization flow through one tested service.</done>
</task>

<task type="auto">
  <name>Task 2: Add safe synchronous POST /generate</name>
  <files>backend/src/api.py</files>
  <read_first>backend/src/generation_service.py, backend/src/config.py, backend/tests/test_api.py, .planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md</read_first>
  <action>Create a FastAPI `app` with a `GenerateRequest` model exposing optional JSON fields `referenceMolPath`, `nSamples`, and `variance` (D-02). Read the approved `nSamples` and `variance` ceilings from `02-01-SUMMARY.md`, define them as named API constants, and use those exact bounds in Pydantic validation; do not substitute the provisional research values unless they are the values explicitly approved in Plan 02-01. Omitted fields use `DEFAULT_NUM_CONFORMERS` and `DEFAULT_VARIANCE`, and rejection remains HTTP 422 without clamping (D-05). Implement `resolve_reference_mol_path(value)` to reject absolute paths and non-`.mol` suffixes, resolve `(REFERENCE_MOLECULES_DIR / value)`, require the resolved candidate to remain under the resolved root (including symlinks), and require a regular existing file; convert every such input failure to concise HTTP 422 detail (D-03). `POST /generate` passes the resolved filesystem path plus the original relative label to `generate_conformer_data`, or passes no path for explicitly labeled DEMO_SMILES fallback (D-04), and returns its `{conformers,count,metadata}` dict. Map unexpected generation exceptions to a concise HTTP 500 message without absolute paths or traceback content. Add no upload route, CORS wildcard, persistence, queue, auth, or deployment behavior.</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/pytest tests/test_api.py -x &amp;&amp; .venv/bin/pytest -q</automated></verify>
  <acceptance_criteria>
    - `backend/src/api.py` exports `app`, `GenerateRequest`, and `resolve_reference_mol_path` and registers exactly `POST /generate` as the Phase 2 application route.
    - Omitted fields reach the stub service with existing defaults; invalid numeric values return 422.
    - Absolute, traversal, symlink-escape, wrong-suffix, and missing paths return 422; a contained nested `.mol` is accepted.
    - Error responses and returned metadata contain no resolved absolute filesystem path.
    - Full backend pytest exits 0 without real model inference.
  </acceptance_criteria>
  <done>The local FastAPI app safely exposes the shared generation path and satisfies all backend contract tests.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>Untrusted local HTTP JSON to Python validation; user-controlled relative path to local filesystem; API to model singleton.</trust_boundaries>
  <threat threat_id="T-02-01" category="Information Disclosure" severity="high" component="referenceMolPath" disposition="mitigate" mitigation_plan="Reject absolute/traversal/wrong-suffix paths, resolve symlinks, enforce resolved-root containment, and return only the original relative label."/>
  <threat threat_id="T-02-02" category="Denial of Service" severity="medium" component="generation parameters" disposition="mitigate" mitigation_plan="Enforce the explicitly approved Plan 02-01 integer ceilings, preserve existing defaults, and bind documented runtime to localhost."/>
  <threat threat_id="T-02-03" category="Information Disclosure" severity="medium" component="exception mapping" disposition="mitigate" mitigation_plan="Return concise HTTP details while retaining technical exceptions only in server logs."/>
  <threat threat_id="T-02-SC" category="Tampering" severity="high" component="FastAPI stack" disposition="mitigate" mitigation_plan="Consume only the human-approved exact pins established by Plan 02-01."/>
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Real backend-generated conformer flow | 02-02 | COVERED | Implements generation endpoint
REQ | — | No formal Phase 2 requirement IDs exist | — | COVERED | Decision IDs used
RESEARCH | — | Thin route to shared service to existing serializer | 02-02 | COVERED | Tasks 1 and 2
RESEARCH | — | Resolved-path containment and concise errors | 02-02 | COVERED | Task 2
RESEARCH | A3 / Open Question 3 | Provisional numeric ceilings must not be silently finalized | 02-02 | COVERED | Task 2 consumes only the explicitly approved guardrails recorded by Plan 02-01
CONTEXT | D-01..D-08 | FastAPI generation contract and metadata | 02-02 | COVERED | Both tasks cite exact decisions
</source_coverage>

<artifacts_this_phase_produces>
- `backend/src/generation_service.py`
- `generate_conformer_data(...)`
- `backend/src/api.py`
- `GenerateRequest`
- `resolve_reference_mol_path(...)`
- FastAPI `app`
- `POST /generate`
</artifacts_this_phase_produces>

<verification>
- [ ] `cd backend && .venv/bin/pytest -q` exits 0.
- [ ] CLI and API import the same `generate_conformer_data` symbol.
- [ ] Unsafe path and invalid numeric cases return 422 in tests.
- [ ] No test loads model weights.
</verification>

<success_criteria>
- D-01 through D-08 are implemented by one shared backend path.
- MolBlock remains the primary response geometry.
- File access is constrained to the allowed reference root.
</success_criteria>

<output>After completion, create `.planning/phases/02-frontend-integration-and-real-data-flow/02-02-SUMMARY.md`.</output>
