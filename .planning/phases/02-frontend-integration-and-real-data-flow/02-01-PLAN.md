---
phase: 02-frontend-integration-and-real-data-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/requirements.txt
  - backend/tests/test_generation_service.py
  - backend/tests/test_api.py
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
user_setup: []
must_haves:
  truths:
    - "FastAPI, Uvicorn, and HTTPX are not installed until a human approves verified package identities and compatible versions."
    - "Numeric request ceilings are explicitly approved as local prototype workload guardrails before tests or endpoint validation hard-code them."
    - "Backend tests define the shared generation and HTTP contracts without loading model weights."
    - "The API contract rejects unsafe paths and invalid numeric inputs before production endpoint code is added."
  artifacts:
    - path: backend/tests/test_generation_service.py
      provides: "Weight-free contract tests for D-01 and D-04 through D-08"
      contains: "test_"
    - path: backend/tests/test_api.py
      provides: "Stubbed endpoint and safe-path tests for D-02, D-03, and D-05"
      contains: "TestClient"
  key_links:
    - from: backend/tests/test_generation_service.py
      to: backend/src/generation_service.py
      via: "imports the planned generate_conformer_data contract"
      pattern: "generation_service"
    - from: backend/tests/test_api.py
      to: backend/src/api.py
      via: "exercises POST /generate with a replaced generation service"
      pattern: "\/generate"
---

<objective>
Establish the approved dependency and test foundation for the Phase 2 backend decisions D-01 through D-08.

Purpose: Enforce the supply-chain checkpoint and make backend behavior test-first without invoking the expensive model.
Output: Human-approved dependency pins plus initially failing service and API contract tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-VALIDATION.md
@backend/pyproject.toml
@backend/requirements.txt
@backend/src/generate_demo.py
@backend/src/serialize.py

<interfaces>
Planned service contract consumed by the Wave 0 tests:
- `generate_conformer_data(reference_mol_path=None, reference_path_label=None, n_samples=DEFAULT_NUM_CONFORMERS, variance=DEFAULT_VARIANCE, diffusion_steps=DEFAULT_DIFFUSION_STEPS) -> dict[str, Any]`
- `POST /generate` accepts `{ referenceMolPath?: string, nSamples?: number, variance?: number }` and returns the existing `{ conformers, count, metadata }` shape.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Verify dependencies and approve prototype numeric guardrails</name>
  <files>backend/pyproject.toml, backend/requirements.txt</files>
  <read_first>backend/pyproject.toml, backend/requirements.txt, .planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md</read_first>
  <action>Before any dependency edit, install, contract-test creation, or endpoint implementation, query PyPI for `fastapi`, `uvicorn`, and `httpx`; inspect each package's canonical project URL, repository ownership, release history, Python 3.10+ compatibility, and mutually compatible current pin. Also inspect the existing generator defaults and installed `mlconfgen` call signature, then present proposed positive upper bounds for `nSamples` and `variance` as explicit local-prototype workload guardrails. The proposal must explain that the limits are API resource controls rather than library/model limits, preserve existing defaults, reject rather than clamp, and identify the exact constants the backend and form will share. Present the three dependency pins and two numeric ceilings to the human for one blocking decision. Do not proceed on an offline/SUS package result or without explicit approval of both the pins and guardrails.</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pip index versions fastapi &amp;&amp; .venv/bin/python -m pip index versions uvicorn &amp;&amp; .venv/bin/python -m pip index versions httpx</automated></verify>
  <acceptance_criteria>
    - The checkpoint displays exact proposed versions for all three packages and their canonical PyPI/repository identities.
    - The checkpoint displays proposed `nSamples` and `variance` ceilings, the unchanged existing defaults, and the resource-guard rationale without representing the ceilings as model-supported maxima.
    - The human explicitly approves the three pins and two numeric ceilings before Task 2 begins.
    - No dependency file changes, package installs, contract-test creation, or production implementation occur before approval.
  </acceptance_criteria>
  <done>Exact legitimate package pins and justified prototype request ceilings are approved and recorded in the summary, or execution remains blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Record the approved runtime and test dependencies</name>
  <files>backend/pyproject.toml, backend/requirements.txt</files>
  <read_first>backend/pyproject.toml, backend/requirements.txt, .planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md</read_first>
  <action>After Task 1 approval only, add the approved exact FastAPI and Uvicorn pins to project runtime dependencies and `requirements.txt`; add the approved exact HTTPX pin to the `dev` optional dependency group and `requirements.txt` beside pytest. Install from `requirements.txt` into `backend/.venv`. Do not add a database, queue, auth, deployment, browser generation, 3Dmol.js, or a second HTTP/client library.</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -c "import fastapi, uvicorn, httpx; print(fastapi.__version__, uvicorn.__version__, httpx.__version__)"</automated></verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains the exact approved FastAPI and Uvicorn runtime pins and HTTPX dev pin.
    - `backend/requirements.txt` contains the same exact pins once each.
    - The import command exits 0 and prints all three installed versions.
  </acceptance_criteria>
  <done>The approved dependency set is reproducible in both backend dependency manifests and imports successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Create failing backend contract tests before production behavior</name>
  <files>backend/tests/test_generation_service.py, backend/tests/test_api.py</files>
  <read_first>backend/src/generate_demo.py, backend/src/serialize.py, backend/src/molecule_utils.py, backend/src/config.py, .planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md, .planning/phases/02-frontend-integration-and-real-data-flow/02-VALIDATION.md</read_first>
  <action>Create `backend/tests/` and two pytest modules. In `test_generation_service.py`, use monkeypatch/fake generator results and small RDKit molecules to specify D-01 and D-04 through D-08: omitted values use existing defaults, demo fallback metadata is explicit, file metadata preserves the relative label, successful values retain order and non-empty `molBlock` plus coordinates, `None` values never become conformers, and metadata has `num_requested`, `num_generated`, `num_failed`, `warnings`, and `warning_count`. In `test_api.py`, use FastAPI `TestClient` and replace the service so no weights load; specify D-02, D-03, and D-05 for omitted fields, `422` validation at the approved numeric ceilings, accepted nested relative `.mol`, and rejection of absolute paths, traversal, symlink escape, wrong suffix, and missing files. Avoid missing-module collection errors: discover planned modules with `importlib.util.find_spec`, conditionally import them only when present, skip implementation-dependent cases while absent, and add explicit sentinel tests whose assertion messages are exactly `Phase 2 production module missing: src.generation_service` and `Phase 2 production module missing: src.api`. This makes the expected-red state collect successfully and fail only on the intentionally absent contracts.</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m py_compile tests/test_generation_service.py tests/test_api.py &amp;&amp; set +e; .venv/bin/pytest tests/test_generation_service.py tests/test_api.py -q &gt;/tmp/mol-card-phase2-wave0.txt 2&gt;&amp;1; status=$?; set -e; test "$status" -eq 1 &amp;&amp; grep -q 'Phase 2 production module missing: src.generation_service' /tmp/mol-card-phase2-wave0.txt &amp;&amp; grep -q 'Phase 2 production module missing: src.api' /tmp/mol-card-phase2-wave0.txt &amp;&amp; ! grep -Eq 'ERROR collecting|SyntaxError|ImportError' /tmp/mol-card-phase2-wave0.txt</automated></verify>
  <acceptance_criteria>
    - Both test files compile and collect tests for every behavior listed in the action.
    - The tests patch generation/model access and never require files under `data/model_weights/`.
    - Pytest exits with code 1 and output contains both exact sentinel messages, with no collection, syntax, or import error.
    - No production backend module is created in this task.
  </acceptance_criteria>
  <done>Wave 0 supplies deterministic red tests for the backend contract before implementation.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>PyPI/network to local virtual environment; test inputs to the future filesystem/API boundary.</trust_boundaries>
  <threat threat_id="T-02-SC" category="Tampering" severity="high" component="backend dependencies" disposition="mitigate" mitigation_plan="Block dependency edits and installation until canonical package identities and exact compatible pins receive explicit human approval."/>
  <threat threat_id="T-02-TEST-WEIGHTS" category="Denial of Service" severity="medium" component="backend tests" disposition="mitigate" mitigation_plan="Stub generator initialization so ordinary tests never load model weights or run inference."/>
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Replace mocks with backend-generated conformers and verify end-to-end | 02-01 | COVERED | Establishes executable backend contract
REQ | — | No formal Phase 2 requirement IDs exist | — | COVERED | Decision IDs are used as instructed
RESEARCH | — | Mandatory legitimacy gate for FastAPI/Uvicorn/HTTPX | 02-01 | COVERED | Task 1 blocks install
RESEARCH | A3 / Open Question 3 | Numeric ceilings require confirmation before hard-coding | 02-01 | COVERED | Task 1 records rationale and requires explicit approval before tests or implementation
RESEARCH | — | Weight-free service and API tests must precede production code | 02-01 | COVERED | Task 3 is Wave 0
CONTEXT | D-01..D-08 | Backend service and HTTP contract | 02-01 | COVERED | Tests specify all backend decisions
</source_coverage>

<artifacts_this_phase_produces>
- `backend/tests/test_generation_service.py`
- `backend/tests/test_api.py`
- Approved FastAPI/Uvicorn/HTTPX pins in `backend/pyproject.toml` and `backend/requirements.txt`
</artifacts_this_phase_produces>

<verification>
- [ ] Package approval occurred before dependency changes.
- [ ] Numeric ceilings and rationale were approved and recorded before contract tests or endpoint implementation.
- [ ] Dependency imports print the approved versions.
- [ ] Both test modules compile, collect, and fail with the two expected sentinel assertions only.
</verification>

<success_criteria>
- All tasks completed in order.
- T-02-SC is mitigated by explicit human approval.
- Wave 0 tests exist before any Phase 2 production behavior.
</success_criteria>

<output>After completion, create `.planning/phases/02-frontend-integration-and-real-data-flow/02-01-SUMMARY.md`.</output>
