---
phase: 02-frontend-integration-and-real-data-flow
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - frontend/src/types/molecule.ts
  - frontend/src/api/generate.ts
  - frontend/src/components/GenerationForm.tsx
  - frontend/src/styles/GenerationForm.css
  - frontend/vite.config.ts
autonomous: true
requirements: [D-02, D-05, D-06, D-07, D-08, D-19, D-20, D-21, D-22, D-23]
user_setup: []
must_haves:
  truths:
    - "The browser can submit the exact optional generate request fields and reject malformed success payloads."
    - "The form visibly explains the allowed reference root and DEMO_SMILES behavior."
    - "Advanced values persist until the user explicitly resets them to backend defaults."
  artifacts:
    - path: frontend/src/api/generate.ts
      provides: "Typed POST /api/generate client and defensive response checks"
      exports: [generateConformers, GenerationApiError]
    - path: frontend/src/components/GenerationForm.tsx
      provides: "Reference, advanced generation, reset, loading, warning, and error controls"
      exports: [GenerationForm]
  key_links:
    - from: frontend/src/components/GenerationForm.tsx
      to: frontend/src/types/molecule.ts
      via: "submits GenerateRequest values"
      pattern: "GenerateRequest"
    - from: frontend/src/api/generate.ts
      to: backend/src/api.py
      via: "POST /api/generate is proxied to backend POST /generate"
      pattern: "\/api\/generate"
---

<objective>
Create the browser-side HTTP contract and generation controls for D-02, D-05 through D-08, and D-19 through D-23.

Purpose: Give the React app a typed, defensive request boundary and reusable form before wiring result state.
Output: Extended domain types, fetch client, generation form, styles, and local Vite proxy.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md
@.planning/phases/02-frontend-integration-and-real-data-flow/02-01-SUMMARY.md
@frontend/src/types/molecule.ts
@frontend/src/App.tsx
@frontend/vite.config.ts
@frontend/package.json

<interfaces>
Existing response fields:
- `Conformer { id, smiles, molBlock, coordinates, num_atoms, metadata? }`
- `ConformerSet { conformers, count, metadata? }`
Backend request fields: `referenceMolPath?`, `nSamples?`, `variance?`.
Backend default values: `nSamples=10`, `variance=2`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define request, metadata, and error-aware API contracts</name>
  <files>frontend/src/types/molecule.ts, frontend/src/api/generate.ts</files>
  <read_first>frontend/src/types/molecule.ts, frontend/src/data/mockMolecules.ts, backend/src/serialize.py, .planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md</read_first>
  <action>Extend `molecule.ts` with `GenerateRequest` containing optional `referenceMolPath`, `nSamples`, and `variance`; add `GenerationMetadata` with `reference_source`, `reference_3d_geometry`, optional `reference_path`, generation counts, `warnings`, and `warning_count`; type both top-level and conformer metadata without removing `molBlock` or coordinate arrays (D-02/D-06/D-07/D-08). Create `api/generate.ts` exporting `generateConformers(request, signal?)` and `GenerationApiError`. POST JSON to `/api/generate`, omit undefined keys, check `response.ok`, parse FastAPI detail defensively, and validate that a success has a conformer array whose entries include string `id`, `smiles`, and non-empty `molBlock`, numeric `num_atoms`, and coordinate arrays before returning it. Throw concise categorized errors while retaining status and technical payload on the error object for console logging by App (D-19). Add no frontend package.</action>
  <verify><automated>cd frontend &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - `molecule.ts` exports `GenerateRequest` and `GenerationMetadata` with the exact backend field names.
    - `generate.ts` exports `generateConformers` and `GenerationApiError` and posts to `/api/generate`.
    - A non-2xx or malformed 2xx response throws without being returned as `ConformerSet`.
    - MolBlock remains required in `Conformer`; no SMILES-only geometry type is introduced.
    - The TypeScript/Vite build exits 0.
  </acceptance_criteria>
  <done>The frontend owns an exact typed request/response boundary that refuses unusable geometry payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Build persistent generation controls</name>
  <files>frontend/src/components/GenerationForm.tsx, frontend/src/styles/GenerationForm.css</files>
  <read_first>frontend/src/types/molecule.ts, frontend/src/styles/App.css, frontend/src/styles/MoleculeCard.css, .planning/phases/02-frontend-integration-and-real-data-flow/02-CONTEXT.md</read_first>
  <action>Create controlled `GenerationForm` props for `onGenerate(request)`, `isGenerating`, optional `error`, and optional warning summary. Keep `referenceMolPath` visible; render `backend/data/reference_molecules/` as a non-editable allowed-root prefix/hint and submit only the relative free-text remainder (D-20/D-21). Explain that blank input omits the field and uses `DEMO_SMILES` (D-22). Put numeric `nSamples` and `variance` controls inside a semantic `details` Advanced section, initialize them to the existing defaults 10 and 2, and enforce the exact approved ceilings recorded in `02-01-SUMMARY.md` so frontend constraints match the API; submit integers. Preserve all local values across success/failure; Reset restores blank path, 10, and 2 (D-23). Disable Generate while pending, use `aria-busy`, show an indeterminate status, concise inline error, and partial-result warning area (D-16/D-19). Keep styling consistent with existing neutral/green card UI and keyboard-visible focus states.</action>
  <verify><automated>cd frontend &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - The form renders visible reference input, allowed-root hint, blank-path demo explanation, Advanced section, Generate, and Reset controls.
    - Submit omits `referenceMolPath` when trimmed input is blank and includes integer advanced values otherwise.
    - Generate is disabled and an accessible pending status is present when `isGenerating` is true.
    - Reset restores blank/10/2; successful submission does not reset values; numeric max attributes equal the approved backend ceilings.
    - The TypeScript/Vite build exits 0.
  </acceptance_criteria>
  <done>All D-20 through D-23 controls are usable, persistent, accessible, and ready for App wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Proxy local browser requests to the localhost API</name>
  <files>frontend/vite.config.ts</files>
  <read_first>frontend/vite.config.ts, frontend/src/api/generate.ts, .planning/phases/02-frontend-integration-and-real-data-flow/02-RESEARCH.md</read_first>
  <action>Add a Vite development proxy for `/api` to `http://127.0.0.1:8000`, with `changeOrigin: false` and a rewrite that strips only the leading `/api`, so `/api/generate` reaches backend `/generate`. Do not add wildcard CORS or expose a production endpoint configuration; Phase 2 is local-only.</action>
  <verify><automated>cd frontend &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - `vite.config.ts` contains target `http://127.0.0.1:8000` under a `/api` proxy.
    - The rewrite maps `/api/generate` to `/generate` without modifying later path segments.
    - No CORS package or wildcard origin is added.
    - The TypeScript/Vite build exits 0.
  </acceptance_criteria>
  <done>The development browser uses a same-origin relative URL while FastAPI stays localhost-only.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>Browser form to fetch client; untrusted HTTP response to React state; Vite proxy to localhost API.</trust_boundaries>
  <threat threat_id="T-02-04" category="Tampering" severity="medium" component="API response" disposition="mitigate" mitigation_plan="Validate success shape and required MolBlock fields before returning data to App."/>
  <threat threat_id="T-02-05" category="Information Disclosure" severity="medium" component="frontend errors" disposition="mitigate" mitigation_plan="Render concise messages and keep technical payload only on the error object for console diagnostics."/>
  <threat threat_id="T-02-SC" category="Tampering" severity="high" component="frontend dependencies" disposition="mitigate" mitigation_plan="Add no frontend dependency; use built-in fetch and existing React/Vite stack."/>
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Connect frontend to real conformer data | 02-03 | COVERED | Creates request boundary and controls
REQ | — | No formal Phase 2 requirement IDs exist | — | COVERED | Decisions used
RESEARCH | — | Relative fetch via Vite proxy and defensive response validation | 02-03 | COVERED | Tasks 1 and 3
RESEARCH | — | No new client state/schema library | 02-03 | COVERED | Built-in fetch and React only
CONTEXT | D-02,D-05..D-08,D-19..D-23 | Request contract, metadata, errors, controls | 02-03 | COVERED | Tasks 1-3
</source_coverage>

<artifacts_this_phase_produces>
- `GenerateRequest`
- `GenerationMetadata`
- `frontend/src/api/generate.ts`
- `generateConformers(...)`
- `GenerationApiError`
- `frontend/src/components/GenerationForm.tsx`
- `GenerationForm`
- `frontend/src/styles/GenerationForm.css`
- Vite `/api` proxy configuration
</artifacts_this_phase_produces>

<verification>
- [ ] `cd frontend && npm run build` exits 0 after each task.
- [ ] Form request keys exactly match backend camelCase names.
- [ ] Browser URL remains `/api/generate` and proxy target remains localhost.
</verification>

<success_criteria>
- Generation controls capture D-20 through D-23 exactly.
- Invalid HTTP payloads cannot replace usable conformer state.
- No new frontend package or broad CORS policy is introduced.
</success_criteria>

<output>After completion, create `.planning/phases/02-frontend-integration-and-real-data-flow/02-03-SUMMARY.md`.</output>
