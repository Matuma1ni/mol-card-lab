---
phase: 03-frontend-wasm-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/public/models/.gitkeep
  - frontend/src/lib/generator.ts
  - frontend/src/lib/generator.test.ts
  - .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
autonomous: false
requirements:
  - P3-01
  - P3-02
  - P3-03
user_setup:
  - service: manual-model-assets
    why: "The two ONNX model weights are separately distributed and must be placed locally without committing them."
    dashboard_config:
      - task: "Obtain egnn_chembl_15_39.onnx and adj_mat_seer_chembl_15_39.onnx, then place them in frontend/public/models/."
must_haves:
  truths:
    - "A supported browser build either runs one local conformer request with the two manual ONNX assets or records a concrete browser/runtime blocker."
    - "The manually supplied model assets cannot be staged by the repository's normal git workflow."
    - "The proof does not create a network or server generation route."
  artifacts:
    - path: "frontend/src/lib/generator.ts"
      provides: "Adapter-private explicit browser runtime proof boundary"
      exports: ["getGeneratorRuntimeStatus", "generateConformers", "resetGeneratorForTests"]
    - path: ".planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md"
      provides: "Recorded browser runtime, asset loading, output or blocker evidence"
    - path: ".gitignore"
      provides: "Ignore rules for browser model assets"
  key_links:
    - from: "frontend/src/lib/generator.ts"
      to: "frontend/public/models/"
      via: "adapter-local Vite base-URL asset resolution passed to the explicit browser runtime"
      pattern: "import\.meta\.env\.BASE_URL"
    - from: "frontend/src/lib/generator.ts"
      to: "mlconfgen"
      via: "the sole low-level import boundary creates MLConformerGenerator with explicit browser runtime inputs"
      pattern: "MLConformerGenerator\.create"
---

<objective>
Prove or decisively block browser-local mlconfgen execution before card integration, while protecting the manually acquired model files.

Purpose: Phase 3 may proceed to UI integration only after a real served-browser runtime and asset-loading result exists.
Output: An ignored model location, an adapter-private proof boundary with tests, and a reproducible runtime-proof record.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-frontend-wasm-generation/03-CONTEXT.md
@.planning/phases/03-frontend-wasm-generation/03-RESEARCH.md
@.planning/phases/03-frontend-wasm-generation/03-PATTERNS.md
@.planning/phases/03-frontend-wasm-generation/03-VALIDATION.md
@frontend/src/lib/rdkit.ts
@frontend/src/lib/rdkit.test.ts

<interfaces>
From frontend/src/lib/rdkit.ts: `loadRDKit()` caches one initialization promise, clears cache after failure, and builds static-asset URLs from `import.meta.env.BASE_URL`; `resetRDKitForTests()` resets module state. The generator boundary must apply the same retry-safe browser-loader pattern.

From the locked Phase 3 contract: UI consumers receive only `GenerateRequest`, `GeneratedConformer`, `GenerateResponse`, a sanitized runtime status/error, and generation functions. Low-level generator, seed, browser ONNX runtime, RDKit runtime, and model locations stay private to generator.ts.
</interfaces>
</context>

<decision_map>
D-01: Use the `mlconfgen` package and keep `MLConformerGenerator` plus `seed` private to the adapter.
D-02: Initialize with `MLConformerGenerator.create({ egnnOnnx, adjMatSeerOnnx, diffusionSteps })`.
D-03: Support referenceContext plus nAtoms or referenceConformer.positions with nSamples and optional variance/diffusionSteps.
D-04: Return the documented normalized GenerateResponse contract.
D-05: Retain MolBlock as authoritative geometry and treat coordinates/SMILES as optional derived metadata.
D-06: Treat filterInvalid short output as successful partial output.
D-07: Manually acquired ONNX weights must never enter git.
D-08: Require an explicit browser-runtime proof and never introduce a server fallback if it fails.
D-09: Exclude inpainting, fine-tuning, and 3D rendering.
</decision_map>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the package legitimacy inputs for the browser proof</name>
  <read_first>
    - frontend/package.json
    - .planning/phases/03-frontend-wasm-generation/03-RESEARCH.md
  </read_first>
  <files>No repository files; this gate authorizes the exact dependency inputs for Task 2.</files>
  <what-built>Nothing is installed before this gate. RESEARCH.md lacks a Package Legitimacy Audit, so mlconfgen and onnxruntime-web are treated as assumed packages.</what-built>
  <action>Inspect and approve the exact package inputs before any install. This checkpoint is mandatory because the research artifact has no Package Legitimacy Audit; it prevents unreviewed package resolution from becoming part of the browser proof.</action>
  <how-to-verify>
    1. Inspect the official npm package pages and the mlconfgen JS-runtime source/release referenced by the project.
    2. Confirm the intended package names, exact versions or compatibility range, package owner/source, and browser-compatible entry point for the chosen runtime.
    3. Confirm that installing those two packages is approved for this prototype.
  </how-to-verify>
  <verify>
    <automated>test -f frontend/package.json && test -f .planning/phases/03-frontend-wasm-generation/03-RESEARCH.md</automated>
    <human-check>Confirm the requested package versions and browser entry-point evidence using the three steps above.</human-check>
  </verify>
  <resume-signal>Type "approved: mlconfgen@... and onnxruntime-web@..." with the approved versions, or describe the issue.</resume-signal>
  <acceptance_criteria>The executor has explicit human approval for the exact package install inputs before changing the lockfile.</acceptance_criteria>
  <done>Approved package names and versions are available to the following proof task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish ignored browser model assets and the explicit runtime proof boundary</name>
  <read_first>
    - .gitignore
    - frontend/package.json
    - frontend/src/lib/rdkit.ts
    - frontend/src/lib/rdkit.test.ts
    - .planning/phases/03-frontend-wasm-generation/03-RESEARCH.md
  </read_first>
  <files>.gitignore, frontend/package.json, frontend/package-lock.json, frontend/public/models/.gitkeep, frontend/src/lib/generator.ts, frontend/src/lib/generator.test.ts, .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md</files>
  <behavior>
    - A failed initialization clears the cached promise so a later local retry begins a new attempt.
    - The explicit browser runtime configuration receives the two Vite-resolved model URLs and creates the generator using D-02.
    - A proof request retains a non-empty MolBlock from every normalized result; a short array resolves with requested and generated counts instead of rejecting.
    - Missing assets and unsupported runtime conditions map to sanitized local failure categories without exposing thrown runtime details.
  </behavior>
  <action>After Task 1 approval, install only the approved mlconfgen and browser ONNX Runtime packages. Add frontend/public/models/ ignore rules for both required ONNX filenames while retaining .gitkeep, per D-07. Implement generator.ts as the sole D-01 adapter boundary: explicitly select/inject the approved browser runtime, resolve model URLs through Vite's base URL, cache initialization with failure reset, invoke D-02 creation, and expose a narrow proof/status surface. Do not expose runtime objects or asset locations outside the module. Write unit tests with adapter-private mocks rather than real WASM. Build, then serve the production bundle with `cd frontend && npm run preview -- --host 127.0.0.1`; use manually supplied ignored assets and one supported D-03 request in that served browser session. Record the browser name and version, package/runtime configuration, the resolved URLs requested for both model assets, the exact request form used, and success evidence containing a representative MolBlock plus coordinate/atom-order observation in 03-RUNTIME-PROOF.md. If no successful result is possible, record the concrete browser/request/asset or runtime blocker and its sanitized unavailable category instead. If the proof is blocked, leave the module able to report that documented unavailable status only; do not add an alternate generation mechanism, per D-08.</action>
  <verify>
    <automated>cd frontend && npm test -- generator.test.ts && npm run build && git check-ignore -v public/models/egnn_chembl_15_39.onnx public/models/adj_mat_seer_chembl_15_39.onnx</automated>
    <human-check>Run `cd frontend && npm run preview -- --host 127.0.0.1`, open the served build in a supported browser, and submit one supported request form using manually supplied ignored assets. Capture the browser name/version, both requested model-asset URLs, the request form, and a representative MolBlock if successful. If unsuccessful, capture the observed blocker and verify the documented unavailable message matches its sanitized category.</human-check>
  </verify>
  <acceptance_criteria>Both named model files are ignored; all low-level runtime imports are confined to generator.ts; the proof record states either a successful served-browser result with MolBlock evidence or a concrete blocker with no fallback path.</acceptance_criteria>
  <done>The decisive browser proof and git protection are complete, and later plans can branch on the recorded runtime status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manually obtained model files → Vite public assets | Untracked binary model inputs become browser-readable static assets. |
| browser UI → generator adapter | UI request data crosses into a package/runtime that can throw low-level errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | frontend/public/models | mitigate | Ignore the two exact model filenames and verify with git check-ignore before the browser smoke. |
| T-03-02 | Information disclosure | generator.ts failure mapping | mitigate | Convert asset/runtime exceptions into a small sanitized category before React can display them. |
| T-03-03 | Denial of service | adapter initialization | mitigate | Share one initialization promise and reset it after rejection so retry does not retain a broken state. |
| T-03-SC | Tampering | npm installs | mitigate | Blocking human package-legitimacy checkpoint precedes installation because the research audit is absent. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Browser-local mlconfgen adapter behind a proof gate | 03-01 through 03-03 | COVERED | Proof, contract, UI and handoff are sequential.
REQ | P3-01 | Adapter-private mlconfgen imports | 03-01, 03-02 | COVERED | D-01 boundary plus architecture test.
REQ | P3-02 | Explicit browser runtime and served asset proof | 03-01 | COVERED | Success or documented blocker.
REQ | P3-03 | Generator creation and ignored manual weights | 03-01 | COVERED | D-02/D-07.
REQ | P3-04 | Normalized request/response contract and both inputs | 03-02 | COVERED | Contract tests.
REQ | P3-05 | MolBlock-first normalization | 03-02 | COVERED | Required normalization assertions.
REQ | P3-06 | Requested/generated count and partial success | 03-02, 03-03 | COVERED | Adapter and status UI.
REQ | P3-07 | Browser-local loading, generation, failure states | 03-03 | COVERED | UI branch is proof-gated.
REQ | P3-08 | Exclude inpainting/IFM merge | 03-02 | COVERED | Contract has no such request surface.
REQ | P3-09 | Exclude backend, persistence, deployment, training, advanced controls | 03-01 through 03-03 | COVERED | No plan task creates those systems.
RESEARCH | — | Vite public asset strategy, retry-safe loader, production browser smoke | 03-01 | COVERED | Reuses RDKit loader pattern and records proof evidence.
RESEARCH | — | Do not force generated output into existing Conformer | 03-02 | COVERED | Separate generated types and explicit card mapping.
CONTEXT | D-01 through D-09 | Locked browser-local adapter and phase fences | 03-01 through 03-03 | COVERED | Decision citations occur in implementation actions.
</source_audit>

<verification>
Run the focused adapter tests and production build. Confirm git ignores each model name. Perform the required served-browser smoke and preserve the recorded success evidence or blocker.
</verification>

<success_criteria>
The phase has a factual browser-runtime decision, protected local model paths, and no UI integration predicated on an unproven Node-oriented default runtime.
</success_criteria>

## Artifacts this phase produces

- An ignored, documented location for the two manually supplied ONNX files.
- A retry-safe, adapter-private browser runtime proof boundary and its focused tests.
- `03-RUNTIME-PROOF.md`, recording the exact viable configuration or the blocker that limits the UI to unavailable-runtime behavior.

<output>
Create `.planning/phases/03-frontend-wasm-generation/03-01-SUMMARY.md` when done.
</output>
