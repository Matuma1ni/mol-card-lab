---
phase: 03-frontend-wasm-generation
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - frontend/src/types/molecule.ts
  - frontend/src/data/mockMolecules.ts
  - frontend/src/lib/generator.ts
  - frontend/src/lib/generator.test.ts
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
  - frontend/src/components/MoleculeCard.tsx
  - frontend/src/components/Molecule2DPreview.tsx
  - frontend/src/styles/App.css
  - .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
  - .planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md
  - .planning/phases/03-frontend-wasm-generation/03-VERIFICATION.md
  - .planning/phases/03-frontend-wasm-generation/03-04-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - P3-01
  - P3-02
  - P3-03
  - P3-04
  - P3-05
  - P3-06
  - P3-07
  - P3-08
  - P3-09
must_haves:
  truths:
    - "The browser-local adapter accepts either supported reference form and returns validated MolBlock-first conformers with accurate requested and generated counts."
    - "Each selectable local fixture supplies hidden valid context data, and the selected fixture invokes the adapter with its context and a fixed internal request count of three."
    - "The UI distinguishes model loading, active generation, complete success, filtered success, zero valid results, unavailable runtime, and sanitized failure while retaining usable card browsing and prior generated selection."
    - "A served production-browser smoke produces factual local-runtime evidence, or records a reproducible blocker without claiming generated geometry."
    - "Phase 4 receives an evidence-backed geometry handoff containing a real representative MolBlock and coordinate-order findings whenever the smoke succeeds."
  artifacts:
    - path: "frontend/src/types/molecule.ts"
      provides: "Separate Phase 3 GenerateRequest, GeneratedConformer, and GenerateResponse contracts plus fixture-only local generation context"
      contains: "GenerateRequest"
    - path: "frontend/src/lib/generator.ts"
      provides: "Adapter-private validation, runtime invocation, and MolBlock-first normalization"
      exports: ["generateConformers", "getGeneratorRuntimeStatus"]
    - path: "frontend/src/App.tsx"
      provides: "Selected-fixture request mapping and complete browser-local generation state flow"
    - path: ".planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md"
      provides: "Factual served-browser asset, request, geometry, or blocker evidence"
    - path: ".planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md"
      provides: "Confirmed Phase 4 geometry and ordering contract"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/data/mockMolecules.ts"
      via: "selected fixture supplies hidden referenceContext and nAtoms for the fixed request"
      pattern: "referenceContext.*nAtoms|nAtoms.*referenceContext"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/generator.ts"
      via: "the App imports only generateConformers and calls it with the selected fixture's normalized request"
      pattern: "generateConformers"
    - from: "frontend/src/lib/generator.ts"
      to: "mlconfgen"
      via: "the adapter-private initialized runtime receives validated request input and invokes its generation method"
      pattern: "generateConformers"
    - from: "frontend/src/lib/generator.ts"
      to: "frontend/src/types/molecule.ts"
      via: "adapter imports and returns the normalized TypeScript contract"
      pattern: "Generate(Request|Response|dConformer)"
---

<objective>
Close the executed Phase 3 proof-gated gaps now that both ignored ONNX assets exist: create the real normalized browser generator, connect it to selected local fixtures and card selection, then replace stale blocker records with factual served-browser evidence.

Purpose: Phase 4 can only evaluate a viewer after Phase 3 has real browser-generated MolBlock geometry and a UI flow that handles filtered results without a backend fallback.
Output: Tested adapter and UI state machine, hidden fixture request data, and updated runtime-proof, handoff, and verification artifacts. Do not create commits, modify historical plan or summary files, or stage any files.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-frontend-wasm-generation/03-CONTEXT.md
@.planning/phases/03-frontend-wasm-generation/03-RESEARCH.md
@.planning/phases/03-frontend-wasm-generation/03-PATTERNS.md
@.planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
@.planning/phases/03-frontend-wasm-generation/03-VERIFICATION.md
@.planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
@.planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md
@.planning/phases/03-frontend-wasm-generation/03-01-SUMMARY.md
@.planning/phases/03-frontend-wasm-generation/03-02-SUMMARY.md
@.planning/phases/03-frontend-wasm-generation/03-03-SUMMARY.md

<interfaces>
`generator.ts` is the sole module allowed to import `mlconfgen`, `onnxruntime-web`, or the RDKit loader. `createGenerator` is the installed public wrapper; its resolved generator must be treated as an adapter-private runtime object. Preserve its current Vite base-URL model URL resolution, RDKit loader injection, shared initialization promise, and retry-after-rejection behavior.

Keep existing `Conformer` as legacy card data. Add separate Phase 3 `GenerateRequest`, `GeneratedConformer`, and `GenerateResponse` types. `GeneratedConformer` requires an id and non-empty MolBlock; SMILES and coordinate triples are optional derived/card-artwork data.

The locked App request is exactly `{ referenceContext: selectedFixture.referenceContext, nAtoms: selectedFixture.nAtoms, nSamples: 3 }`. The UI may not expose reference conformers, variance, diffusion steps, seed, model paths, runtime controls, or files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and test the validated MolBlock-first adapter and fixture contract</name>
  <read_first>
    - frontend/src/lib/generator.ts
    - frontend/src/lib/generator.test.ts
    - frontend/src/lib/mlconfgen.d.ts
    - frontend/src/lib/rdkit.ts
    - frontend/src/types/molecule.ts
    - frontend/src/data/mockMolecules.ts
    - .planning/phases/03-frontend-wasm-generation/03-CONTEXT.md
    - .planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
  </read_first>
  <files>frontend/src/types/molecule.ts, frontend/src/data/mockMolecules.ts, frontend/src/lib/generator.ts, frontend/src/lib/generator.test.ts, frontend/src/lib/mlconfgen.d.ts</files>
  <behavior>
    - Each of the ten local fixtures has a hidden tuple of exactly three finite numbers and a positive integer atom count.
    - A context request accepts only `referenceContext` plus `nAtoms`; a conformer request accepts only `referenceConformer.positions`; mixed, missing, non-finite, malformed, and non-positive inputs fail through a sanitized input category without invoking the runtime.
    - Valid requests forward nSamples, optional variance and diffusionSteps, and filterInvalid true to the adapter-private runtime `generateConformers` call.
    - Every fake runtime molecule must yield a non-empty `toMolBlock()` result; normalized output retains that MolBlock, derives coordinate triples only when available in the same atom order, and retains requested/generated counts.
    - A runtime result shorter than requested resolves as a successful response, while zero results remains a completed successful response with generated count zero.
  </behavior>
  <action>Use a RED-GREEN-REFACTOR cycle to replace the unavailable-only stub with the real D-03 and D-04 contract. Add typed request/response and fixture context types without widening legacy `Conformer`; add fixed valid context data to every local fixture per the locked UI-SPEC decision. In generator.ts validate that exactly one D-03 reference form is present, translate it into the installed runtime call, pass `filterInvalid: true`, and invoke the initialized runtime's generation method. Normalize returned molecule objects with D-05 `toMolBlock()` as required authoritative geometry; reject invalid or empty MolBlocks via a sanitized adapter error. Preserve array order, derive coordinate triples only from returned geometry when this can be done without changing atom order, and produce D-06 metadata including effective parameters, generation source, numRequested, and numGenerated. Update the local mlconfgen declaration only to accurately type the private runtime call needed by this implementation. Keep runtime objects, model URLs, seed, ONNX runtime, and RDKit APIs out of exported UI types; do not add a server route, synthetic generated results, inpainting, IFM merge, fine-tuning, persistence, or commits.</action>
  <verify>
    <automated>cd frontend && npm test -- generator.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>Both documented reference forms are tested through the private runtime seam; successful responses contain only MolBlock-first normalized records and accurate counts; all ten UI fixtures expose valid hidden request context; filtered and zero-result completions are not represented as generation errors.</acceptance_criteria>
  <done>The adapter can perform real local generation from either supported request form and React has fixed, valid selected-fixture context to use without exposing runtime details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire selected-fixture generation states and generated-card selection</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/App.test.tsx
    - frontend/src/components/MoleculeCard.tsx
    - frontend/src/components/Molecule2DPreview.tsx
    - frontend/src/styles/App.css
    - frontend/src/types/molecule.ts
    - frontend/src/data/mockMolecules.ts
    - frontend/src/lib/generator.ts
    - .planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/components/MoleculeCard.tsx, frontend/src/components/Molecule2DPreview.tsx, frontend/src/styles/App.css</files>
  <behavior>
    - The initially selected local fixture calls the adapter only after the user presses Generate conformers, with its hidden context and nSamples 3; an absent or invalid fixture enters the specified sanitized failure state without a call.
    - Deferred adapter tests prove model-loading and generation-in-progress copy, disabled generation/selector controls, preserved prior card, and stale asynchronous work cannot replace a newer result.
    - Full success selects the first returned conformer; filtered partial success shows requested/generated/filtered counts and preserves returned ordering; zero valid results preserves the prior generated selection and enables another attempt.
    - Failure and unavailable categories show approved sanitized copy, keep mock browsing and the prior generated result available, and expose retry only where retryable.
    - When two or more results exist, an accessible ordinal selector changes the rendered generated card; absent generated SMILES uses the existing 2D-artwork fallback without invalidating its MolBlock.
  </behavior>
  <action>Refactor App into one discriminated generation state owned by the App while retaining existing mock-card preload behavior. Import only the normalized adapter functions/types, validate the selected fixture locally, and call the adapter using the exact locked UI-SPEC request mapping with internal `nSamples: 3`. Implement D-06 counts and all UI-SPEC states: runtime unavailable, model loading, generation in progress, complete success, filtered success, completed zero-result, and sanitized retryable failure. Map each GeneratedConformer explicitly to the existing card rendering seam; preserve prior generated selection and mock browsing during pending, zero, and failure outcomes. Add a compact accessible selector only for multiple results and make its ordinal/text selection visible. Update MoleculeCard and Molecule2DPreview only as needed to accept a generated record's optional SMILES and retain the approved 2D fallback when it is absent; MolBlock remains stored as geometry rather than being rendered in this phase. Match the status roles, copy, focus behavior, disabled controls, reduced-motion behavior, palette, and 44px controls in the UI-SPEC. Do not import low-level runtime packages into React, display raw exception/model details, create a 3D viewer, add user-facing advanced controls, call an HTTP generator, or commit files.</action>
  <verify>
    <automated>cd frontend && npm test -- App.test.tsx generator.test.ts Molecule2DPreview.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>App tests exercise every required state transition and generated selector; a successful generated record reaches the existing card flow through the adapter contract only; partial and zero results are distinguishable from failures and no state removes usable existing browsing.</acceptance_criteria>
  <done>The React stage makes a real local request from the selected fixture and safely displays/selects normalized results without exposing generator internals or adding a backend boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Run the served-browser smoke and replace stale proof, handoff, and verification records</name>
  <read_first>
    - frontend/src/lib/generator.ts
    - frontend/src/App.tsx
    - frontend/public/models/egnn_chembl_15_39.onnx
    - frontend/public/models/adj_mat_seer_chembl_15_39.onnx
    - .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
    - .planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md
    - .planning/phases/03-frontend-wasm-generation/03-VERIFICATION.md
    - .planning/phases/03-frontend-wasm-generation/03-VALIDATION.md
  </read_first>
  <files>.planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md, .planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md, .planning/phases/03-frontend-wasm-generation/03-VERIFICATION.md, .planning/phases/03-frontend-wasm-generation/03-04-SUMMARY.md</files>
  <action>After Tasks 1 and 2 pass automated validation, run the production build and start Vite preview bound to 127.0.0.1. In one supported browser, open the served app, select a fixture, run the fixed three-sample request, and inspect browser network evidence that both named ONNX URLs return successful responses from the served build. Record the exact browser name/version, Vite preview URL, both asset request URLs and statuses, selected fixture identifier, exact context-form request mapping, returned requested/generated counts, a representative real MolBlock emitted by the adapter, and whether optional coordinates were available and match MolBlock atom order. Exercise and record a filtered-short or zero-result branch when the runtime naturally produces it; do not fabricate one solely for documentation. If generation cannot complete, record reproducible steps, actual browser console/network symptom, sanitized UI category, and why no generated-geometry claim can be made. Update RUNTIME-PROOF, PHASE4-HANDOFF, and VERIFICATION to replace the obsolete missing-assets statements with only observed evidence, retain any unresolved blocker accurately, and state that model assets remain ignored. Create this plan's 03-04-SUMMARY.md from the execution result. Do not revise historical summaries or plans, stage files, commit, or introduce a backend/API fallback.</action>
  <verify>
    <automated>cd frontend && npm test && npm run build && git check-ignore -v public/models/egnn_chembl_15_39.onnx public/models/adj_mat_seer_chembl_15_39.onnx && rg -n "browser name|browser version|asset request|MolBlock|atom order|blocker" ../.planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md ../.planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>The three evidence files no longer state that supplied ONNX assets are absent; they contain either all required factual served-browser success evidence and a Phase 4 geometry contract, or a reproducible current blocker with no false success claim. The two model files remain ignored and no commit exists from this plan.</acceptance_criteria>
  <done>Phase 3 has a reproducible browser-local runtime verdict and an evidence-backed handoff that allows Phase 4 to use actual generated geometry only when it exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected fixture → generator adapter | Fixed frontend data is validated before it can trigger resource-intensive browser model work. |
| adapter-private runtime → normalized response | Third-party runtime output and exceptions are converted into safe frontend data and categories. |
| ignored ONNX files → Vite preview browser | Locally supplied binary weights become static browser assets but remain outside Git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | GenerateRequest validation | mitigate | Validate exactly one reference representation, finite coordinates/context, positive atom count, and positive request count before calling the runtime. |
| T-03-05 | Information disclosure | generator and App failure handling | mitigate | Map low-level failures to approved sanitized categories; render no exception, model URL, or runtime object. |
| T-03-06 | Tampering | normalized conformer geometry | mitigate | Require a non-empty MolBlock for every normalized output and retain result order; optional coordinates are derived and checked against atom order. |
| T-03-07 | Denial of service | App generation controls | mitigate | Disable duplicate requests and result selection while a request is active; retain prior useful results on failure or zero output. |
| T-03-08 | Spoofing | Phase 4 evidence | mitigate | Record actual served-browser asset statuses, request mapping, generated MolBlock, and browser version, or label the attempt as a blocker. |
| T-03-09 | Tampering | manual model assets | mitigate | Verify the two exact model paths remain ignored after smoke testing. |
| T-03-SC | Tampering | package installation | accept | No package installation is planned; existing locked dependencies remain unchanged. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Browser-local mlconfgen adapter behind a small UI-safe contract | 03-04 | COVERED | Adapter, selected fixture wiring, and served proof close the verified gaps.
REQ | P3-01 | Adapter-private mlconfgen imports | 03-04 Task 1 | COVERED | Runtime remains private to generator.ts.
REQ | P3-02 | Explicit browser runtime and served asset proof | 03-04 Task 3 | COVERED | Requires real asset requests and a fact-only result or blocker.
REQ | P3-03 | Generator creation with ignored manual weights | 03-04 Tasks 1, 3 | COVERED | Existing creation is used; smoke confirms assets while ignore check remains required.
REQ | P3-04 | Normalized contract and both input forms | 03-04 Task 1 | COVERED | Tests validate/translate both reference forms.
REQ | P3-05 | MolBlock-first normalization | 03-04 Task 1 | COVERED | Every result requires non-empty toMolBlock output.
REQ | P3-06 | Counts and filtered partial success | 03-04 Tasks 1, 2 | COVERED | Adapter metadata and UI states both distinguish partial and zero output.
REQ | P3-07 | Browser-local loading, generation, and failure states | 03-04 Task 2 | COVERED | All UI-SPEC state flows are tested.
REQ | P3-08 | Exclude inpainting and IFM merge | 03-04 Task 1 | COVERED | No contract fields or calls add those runtime features.
REQ | P3-09 | Exclude backend, persistence, deployment, training, advanced controls | 03-04 Tasks 1-3 | COVERED | Actions explicitly retain frontend-only boundaries.
RESEARCH | — | Vite asset strategy, retry-safe loader, and served production smoke | 03-04 Tasks 1, 3 | COVERED | Preserves the RDKit-style loader pattern and tests real preview behavior.
RESEARCH | — | Separate generated contract and explicit card mapping | 03-04 Tasks 1, 2 | COVERED | Does not overload legacy Conformer.
CONTEXT | D-01 | Private mlconfgen/seed adapter boundary | 03-04 Task 1 | COVERED | Explicit action citation.
CONTEXT | D-02 | Explicit creation inputs | 03-04 Task 1 | COVERED | Existing initialization remains adapter-private.
CONTEXT | D-03 | Both reference forms | 03-04 Task 1 | COVERED | Validated and tested translation.
CONTEXT | D-04 | Normalized response contract | 03-04 Task 1 | COVERED | New types and metadata.
CONTEXT | D-05 | MolBlock authority | 03-04 Tasks 1, 3 | COVERED | Normalization and atom-order evidence.
CONTEXT | D-06 | Filtered output success | 03-04 Tasks 1, 2 | COVERED | Count semantics in adapter and UI.
CONTEXT | D-07 | Weights excluded from Git | 03-04 Task 3 | COVERED | Exact post-smoke ignore verification.
CONTEXT | D-08 | Browser proof; no fallback | 03-04 Task 3 | COVERED | Factual proof or reproducible blocker only.
CONTEXT | D-09 | No inpainting, fine-tuning, or 3D rendering | 03-04 Tasks 1, 2 | COVERED | Scope fences retained.
CONTEXT | UI-SPEC revision | Fixture-owned hidden context, nAtoms, fixed nSamples 3 | 03-04 Tasks 1, 2 | COVERED | Implemented exactly in fixture types and App mapping.
</source_audit>

<verification>
Run focused adapter and UI tests after their tasks, then `cd frontend && npm test && npm run build`. Confirm exact model ignore rules. Complete the served Vite production-browser smoke and ensure the evidence documents either real generated geometry or a reproducible failure without a fabricated success claim.
</verification>

<success_criteria>
The only production generation path is selected local fixture → normalized adapter → browser-local runtime → MolBlock-first result → generated card selection. All allowed UI outcomes are accessible and sanitized. Phase 4 has current, factual browser and geometry evidence or a precise blocker record.
</success_criteria>

## Artifacts this gap closure produces

- Separate, validated Phase 3 request/response and fixture-generation types.
- A real `generateConformers(request)` runtime call with MolBlock-first normalization and filtered-count metadata.
- A selected-fixture generation state machine and generated-card selector.
- Current factual runtime proof, Phase 4 geometry handoff, and verification record.

<output>
Create `.planning/phases/03-frontend-wasm-generation/03-04-SUMMARY.md` when done. Do not commit it or any plan artifact because this gap-closure plan has an explicit no-commit constraint.
</output>
