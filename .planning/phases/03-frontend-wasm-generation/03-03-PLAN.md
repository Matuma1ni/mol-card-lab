---
phase: 03-frontend-wasm-generation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
  - frontend/src/components/MoleculeCard.tsx
  - frontend/src/components/Molecule2DPreview.tsx
  - frontend/src/components/Molecule2DPreview.test.tsx
  - frontend/src/styles/App.css
  - .planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md
autonomous: true
requirements:
  - P3-04
  - P3-05
  - P3-06
  - P3-07
  - P3-08
  - P3-09
must_haves:
  truths:
    - "A user sees local model preparation, generation, full success, filtered partial success, zero valid result, and sanitized failure states without losing mock-card browsing."
    - "When runtime proof succeeds, the first normalized generated conformer enters the existing card-selection flow and later results can be selected."
    - "When runtime proof is unavailable, the action is disabled with the specified local-only explanation and no alternate generation path."
    - "Phase 4 receives recorded geometry/runtime evidence rather than a new 3D viewer."
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Proof-gated generation state machine and generated result selection"
    - path: "frontend/src/styles/App.css"
      provides: "Approved compact generation status treatment"
    - path: ".planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md"
      provides: "Geometry, browser runtime, and filtered-order handoff evidence"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/generator.ts"
      via: "imports only normalized adapter contract/status and invokes generation from the primary action"
      pattern: "generateConformers"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/MoleculeCard.tsx"
      via: "maps a selected normalized conformer to card-compatible optional-SMILES display data"
      pattern: "MoleculeCard"
---

<objective>
Wire the proof-gated normalized adapter into the collectible-card flow and complete the Phase 4 geometry handoff without adding a viewer or network boundary.

Purpose: A browser-supported runtime becomes a clear local generation interaction; a blocked runtime becomes a clear, usable unavailable state.
Output: Accessible status/result UI, focused integration tests, optional-SMILES card fallback, and geometry handoff evidence.
</objective>

<execution_context>
@/Users/nadezhdavladimirova/.codex/gsd-core/workflows/execute-plan.md
@/Users/nadezhdavladimirova/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-frontend-wasm-generation/03-CONTEXT.md
@.planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
@.planning/phases/03-frontend-wasm-generation/03-VALIDATION.md
@.planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
@frontend/src/App.tsx
@frontend/src/App.test.tsx
@frontend/src/components/MoleculeCard.tsx
@frontend/src/components/Molecule2DPreview.tsx
@frontend/src/styles/App.css

<interfaces>
App may import only adapter-facing `generateConformers`, its normalized types, and sanitized runtime status. It must not import a model asset URL, mlconfgen symbol, seed, ONNX Runtime, or RDKit runtime API.

UI state is a discriminated App-level state: unavailable, model-loading, generating, success, partial-success, zero-result, or failure. A response with numGenerated below numRequested is partial-success, while zero-result is a completed neutral state; neither is a thrown runtime failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add proof-gated accessible generation states and card handoff</name>
  <read_first>
    - .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
    - .planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
    - frontend/src/App.tsx
    - frontend/src/App.test.tsx
    - frontend/src/components/MoleculeCard.tsx
    - frontend/src/components/Molecule2DPreview.tsx
    - frontend/src/styles/App.css
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/components/MoleculeCard.tsx, frontend/src/components/Molecule2DPreview.tsx, frontend/src/components/Molecule2DPreview.test.tsx, frontend/src/styles/App.css</files>
  <behavior>
    - Runtime-unavailable shows `Local generator unavailable`, disables Generate conformers, preserves mock browsing, and announces the local-only constraint.
    - Loading and generation keep the current card visible and disable only generation/result controls while using aria-busy plus role=status.
    - Full and filtered success select the first returned result deterministically; filtered success says how many invalid conformers were filtered and is not rendered as an error.
    - A successful zero result preserves the previous card and permits another attempt; a sanitized failure uses role=alert, Retry generation, and never reveals a stack trace.
    - A generated conformer lacking SMILES keeps the card shell and existing `2D preview unavailable` artwork fallback; its MolBlock remains successful geometry for Phase 4.
  </behavior>
  <action>Read the proof record before editing. If it supports runtime generation, import only the normalized adapter contract into App and add the D-04 state flow plus one 44px minimum `Generate conformers` action, compact status block, and ordinal result selector specified by UI-SPEC. Map generated results explicitly into card display props; narrow MoleculeCard/Molecule2DPreview so missing optional SMILES yields the mandated artwork fallback without treating a valid D-05 MolBlock as failure. Implement the approved exact status copy, accessible roles, keyboard-invoked success focus handling, and reduced-motion behavior in App CSS. If the proof is blocked, implement only the D-08 unavailable state and its documented sanitized reason; preserve the existing mock-card selection flow and do not present a fake generator, alternative runtime, HTTP request, viewer, inpainting, or fine-tuning control. Add tests for every required state and D-06 partial-output behavior.</action>
  <verify>
    <automated>cd frontend && npm test -- App.test.tsx Molecule2DPreview.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>The UI uses only normalized adapter exports; all specified status states have accessible copy; partial output remains success; unavailable mode leaves mock browsing functional and offers no network route; no 3D region is added.</acceptance_criteria>
  <done>The existing centered card remains the visual focus while users can understand and act on local generation outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Phase 4 geometry and browser-runtime handoff</name>
  <read_first>
    - .planning/phases/03-frontend-wasm-generation/03-RUNTIME-PROOF.md
    - .planning/phases/03-frontend-wasm-generation/03-UI-SPEC.md
    - frontend/src/lib/generator.ts
    - frontend/src/App.tsx
  </read_first>
  <files>.planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md</files>
  <action>Create the concise Phase 4 handoff from the proof and implemented adapter: record the browser and explicit ONNX runtime configuration, Vite asset strategy, representative normalized MolBlock or documented unavailable blocker, optional coordinate derivation and atom-order relationship, filtered-result ordering/count semantics, sanitized UI failure categories, and the fact that Phase 3 provides no viewer. For a blocked proof, state that Phase 4 cannot select a viewer against generated geometry yet and cite the exact runtime blocker; retain D-08's browser-only boundary rather than proposing a server solution. Cover D-05, D-06, D-08, and D-09 explicitly.</action>
  <verify>
    <automated>test -s .planning/phases/03-frontend-wasm-generation/03-PHASE4-HANDOFF.md && cd frontend && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>The handoff makes Phase 4's viewer evaluation possible after a successful proof, or precisely explains why generated geometry is unavailable after a blocked proof; it does not add a 3D implementation.</acceptance_criteria>
  <done>Phase 4 has the documented runtime and geometry facts it needs, with no ambiguity between partial success and generation failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter status/result → React state | UI must not display low-level errors or misrepresent completed partial output. |
| normalized conformer → 2D card artwork | Optional SMILES is not authoritative generated geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information disclosure | status/error UI | mitigate | Render only adapter-sanitized messages; tests forbid stack-trace display behavior. |
| T-03-08 | Spoofing | generation status | mitigate | Copy consistently states local/on-device execution and never claims upload, saving, validation, or 3D visibility. |
| T-03-09 | Tampering | card mapping | mitigate | Keep MolBlock in normalized generated data and treat absent SMILES as artwork fallback only. |
| T-03-10 | Denial of service | UI controls | mitigate | Disable generation/result controls while active and retain prior successful/mock card on zero result or failure. |
| T-03-SC | Tampering | package/runtime imports | mitigate | Consume only the Plan 02 normalized adapter contract in UI modules. |
</threat_model>

<verification>
Run the focused App and preview tests, then the complete frontend test suite and production build. Perform the browser smoke specified in Plan 01 when generation is supported; verify unavailable mode in the served build when it is not.
</verification>

<success_criteria>
The app communicates all local generation outcomes clearly, never mistakes filtering for failure, preserves MolBlock-first output for the next phase, and remains frontend-only without a viewer.
</success_criteria>

## Artifacts this phase produces

- A proof-gated Generate conformers interaction with accessible status, retry, partial-result, and selector behavior.
- Card artwork fallback that does not invalidate a generated conformer lacking optional SMILES.
- `03-PHASE4-HANDOFF.md` with browser runtime, geometry, ordering, and blocker facts for the later viewer phase.

<output>
Create `.planning/phases/03-frontend-wasm-generation/03-03-SUMMARY.md` when done.
</output>
